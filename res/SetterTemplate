    private var _${name}:${type};

    public function set${u_name}(value:${type}):I${model_name}
    {
        _${name} = value;

        dispatchMessage(${enum_name}MessageType.${message_type});

        return this;
    }