${imports}

class ${model_name} extends ${model_base_name} implements I${model_name}
{
    @Inject
    private var ${data}:${typedef_name};

${content}

    @PostConstruct
    ${_override}private function init():Void
    {
        ${assign}
        ${_super}
    }
}